name: Build
on: [push]

jobs:
  build-ffmpeg:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v4

      - name: setup java
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: setup android sdk
        uses: android-actions/setup-android@v4

      - name: run sh
        run: "./ffmpeg-android-maker"
